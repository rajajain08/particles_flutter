{
  "buildCommand": "git clone https://github.com/flutter/flutter.git --depth 1 --branch 3.24.0 $HOME/flutter || true && export PATH=\"$HOME/flutter/bin:$PATH\" && flutter config --enable-web && cd particles_flutter/example && flutter pub get && flutter build web --release",
  "outputDirectory": "particles_flutter/example/build/web",
  "installCommand": "echo 'skip'"
}
